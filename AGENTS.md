# Star KB agent instructions

Star KB is the shared durable symbolic knowledge system for StarIntel.

## Core rule

Do not skip the RAGE gates.

Research -> explicit research approval -> design -> explicit design approval -> implementation -> verification.

A green build is evidence, not approval.

## Current phase

The initial shared-KB research and Prolog corpus/query design are approved. The currently authorized implementation slice is `prolog-star-kb`: deterministic canonical StarIntel JSON -> Prolog projection, provenance-aware semantic indexes, bounded AI reasoning tools, and tests.

Do not bulk-populate the corpus with real-world data as part of this slice.

## Source of truth

The Org tree under `roam/` is authoritative for project state:

- `roam/indexes/star-kb.org` — project index and current gate state
- `roam/research/star-kb/` — approved and active research
- `roam/design/star-kb/` — approved/reviewable implementation designs
- `roam/implement/` — active implementation tracking when promoted

Read the active note, its direct links, the project index, relevant source, and relevant tests. Do not dump the whole repository into context for ordinary focused work.

## Knowledge architecture constraints

- Canonical StarIntel JSON is the authoritative durable record.
- Prolog is the deterministic symbolic/reasoning projection of that JSON, not a second independent truth store.
- The active additive StarIntel release can advance while the immutable base schema remains unchanged; record release version, base schema version, profile version, schema revision, and expansion identity in generated projections.
- Every JSON field must remain representable in the projection. Unknown/additive fields must not disappear merely because semantic indexing code does not know them yet.
- Facts imported from external sources or generated by models must carry provenance in canonical StarIntel JSON before promotion.
- Stored source facts and derived conclusions must remain distinguishable.
- Public reasoning predicates are contracts; breaking semantic changes require explicit versioning/migration.
- Knowledge is modular. Do not create a monolithic global file.
- Actors should consume shared knowledge through bounded semantic query/tool contracts rather than arbitrary Prolog goals or filesystem guessing.
- Learned/neuro-symbolic output is candidate/generated knowledge by default, not trusted canonical truth.
- StarLang should bind to the same projection/query contract rather than create a parallel ontology.

## AI/RLM boundary

AI-facing tools expose named operations such as graph traversal, explanation, contradiction search, timeline, provenance, comparison, why-not, and reasoning-capability routing.

Do not expose unrestricted remote Prolog evaluation. Tool calls must compile into bounded exported predicates with validated arguments and resource limits.

RLM/Skynet may choose reasoning capabilities, branch investigations, request evidence, and combine results. It may not silently turn generated hypotheses into canonical facts.

## Durable agent knowledge

When work discovers reusable debugging knowledge, invariants, schema rules, tool behavior, or repository conventions, preserve it in durable project knowledge rather than only in chat text.

Prefer many focused Prolog modules over one growing catch-all file. Runtime observations tied to a specific run or checkout must not silently become canonical domain facts.

## Working rules

- Keep changes bounded to the current approved slice.
- Reuse or extend existing tools before creating parallel machinery.
- Add tests for behavior changes.
- Do not claim validation passed unless the exact command was run successfully.
- Keep secrets, generated runtime artifacts, corpus projections, and caches out of Git.

## Validation

Run the repository validation commands before publishing changes:

```sh
python3 scripts/sync.py
python3 scripts/sync.py --check
python3 scripts/validate-docs.py
python3 -m unittest discover -s tests -p 'test_*.py' -v
```

When SWI-Prolog is available, also load `kb/core/star_json.pl` and `kb/core/star_reasoning.pl` and execute the Prolog smoke suite.

## Current next action

Finish and review the bounded `prolog-star-kb` JSON projection + reasoning-tool slice. Do not bulk-ingest data until this slice is verified and the next gate is explicitly authorized.
